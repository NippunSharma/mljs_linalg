{
  "name": "@webml/linalg",
  "version": "1.0.0",
  "description": "A package for linear algebra in the browser !!",
  "main": "dist-module/src/types/index.js",
  "types": "dist-module/src/types/index.d.ts",
  "scripts": {
    "build": "shx mkdir -p dist-module && tsc --outdir dist-module && shx cp resources/webml_linalg.wasm dist-module/resources/webml_linalg.wasm",
    "clean": "shx rm -rf dist-module",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": [
    "linear-algebra",
    "linear",
    "algebra",
    "wasm",
    "browser",
    "c++",
    "matrix"
  ],
  "author": "",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
