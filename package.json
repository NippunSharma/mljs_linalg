{
  "name": "@webml/linalg",
  "version": "1.0.0",
  "description": "A package for linear algebra in the browser !!",
  "main": "dist/src/types/index.js",
  "types": "dist/src/types/index.d.ts",
  "scripts": {
    "build": "mkdir -p dist && tsc && cp resources/webml_linalg.wasm dist/resources/webml_linalg.wasm",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": [
    "linear-algebra",
    "linear",
    "algebra",
    "wasm",
    "browser",
    "c++",
    "matrix"
  ],
  "author": "",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
