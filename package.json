{
  "name": "@ml.js/linalg",
  "version": "0.1.0-beta.2",
  "repository": {
    "type": "git",
    "url": "https://github.com/NippunSharma/mljs_linalg"
  },
  "description": "A package for linear algebra in the browser !!",
  "main": "dist/src/types/index.js",
  "types": "dist/src/types/index.d.ts",
  "scripts": {
    "build": "mkdir -p dist && tsc",
    "clean": "rm -rf dist",
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "coverage": "nyc -r lcov -e .ts -x \"*.test.ts\" npm run test",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": [
    "linear-algebra",
    "linear",
    "algebra",
    "wasm",
    "browser",
    "c++",
    "matrix"
  ],
  "author": "Nippun Sharma",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "mocha": "^10.1.0",
    "nyc": "^15.1.0",
    "shx": "^0.3.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "engines": {
    "node": ">=17.5.0"
  }
}
